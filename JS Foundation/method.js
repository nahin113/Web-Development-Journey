const calculator = {
    add: function(a,b) {
        return a+b;
    },
    sub: function(a,b) {
        return a-b;
    },
}

console.log(calculator.add(5, 8));

console.log(Math.floor(Math.random()*5)+1);

let Person = {
    name : "Nahin",
    city : "Dhaka" ,
    Age : 25
}

Person.name = "Sadika"
console.log(Person.name);
